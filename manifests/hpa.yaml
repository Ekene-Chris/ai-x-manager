apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: ai-app
  labels:
    app.kubernetes.io/name: ai-app
    app.kubernetes.io/part-of: ai-app-project
    kubernetes.azure.com/generator: devhub
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: ai-app
  minReplicas: 1
  maxReplicas: 5
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 80