apiVersion: v1
kind: Service
metadata:
  name: ai-app
  namespace: ai-app
  labels:
    app.kubernetes.io/name: ai-app
    kubernetes.azure.com/generator: devhub
spec:
  type: LoadBalancer
  selector:
    app.kubernetes.io/name: ai-app
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000